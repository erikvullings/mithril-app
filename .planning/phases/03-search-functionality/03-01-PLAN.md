---
phase: 03-search-functionality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/erik.vullings/dev/mithril-app/packages/gui/src/services/meiosis.ts
  - /Users/erik.vullings/dev/mithril-app/packages/gui/src/components/layout.ts
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
must_haves:
  truths:
    - "User can enter a search query and see filtered results appear immediately"
    - "Search results update dynamically as the user types or modifies the query"
    - "Empty search returns appropriate message (no items found)"
  artifacts:
    - path: "/Users/erik.vullings/dev/mithril-app/packages/gui/src/services/meiosis.ts"
      provides: "setSearchResults service with filtering logic"
      contains: "export const setSearchResults"
    - path: "/Users/erik.vullings/dev/mithril-app/packages/gui/src/components/layout.ts"
      provides: "Search dialog UI with clear button and results display"
      contains: "m(TextInput" and "No results found"
  key_links:
    - from: "/Users/erik.vullings/dev/mithril-app/packages/gui/src/components/layout.ts"
      to: "/Users/erik.vullings/dev/mithril-app/packages/gui/src/services/meiosis.ts"
      via: "searchFilter state and searchResults state"
      pattern: "searchFilter.*attrs.*state|searchResults.*attrs.*state"
    - from: "/Users/erik.vullings/dev/mithril-app/packages/gui/src/services/meiosis.ts"
      to: "localStorage"
      via: "model data retrieval for filtering"
      pattern: "localStorage.*MITHRIL_APP_MODEL"
---

<objective>
Fix the search functionality to enable dynamic filtering and result display

Purpose: The current search implementation has a placeholder service that always returns empty results. This plan fixes the core search logic and updates the UI to properly display results.

Output:
- `packages/gui/src/services/meiosis.ts` - Updated setSearchResults service with filtering logic
- `packages/gui/src/components/layout.ts` - Fixed search results display and added clear button
</objective>

<execution_context>
@/Users/erik.vullings/dev/mithril-app/packages/gui/src/services/meiosis.ts
@/Users/erik.vullings/dev/mithril-app/packages/gui/src/components/layout.ts
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-search-functionality/03-CONTEXT.md

# Existing Code

## Search Results Service (meiosis.ts lines 87-95)
```typescript
export const setSearchResults: Service<State> = {
  onchange: (state) => state.searchFilter,
  run: (cell) => {
    const state = cell.getState();
    const { searchFilter } = state;
    console.log(`Searching ${searchFilter}`);
    cell.update({ searchResults: () => [] });
  },
};
```

## Search Modal UI (layout.ts lines 165-193)
```typescript
m(
  '.container',
  children,
  searchDialogOpen &&
    m(ModalPanel, {
      id: 'search-dialog',
      title: t('SEARCH'),
      isOpen: searchDialogOpen,
      onToggle: (open) => {
        searchDialogOpen = open;
      },
      description: m('.modal-content.row', [
        m(TextInput, {
          id: 'search',
          label: t('SEARCH'),
          iconName: 'search',
          initialValue: searchFilter,
          autofocus: true,
          onchange: (v) => {
            actions.setSearchFilter(attrs, v);
          },
        }),
        searchFilter &&
          searchResults && [
            m('pre.col.s12', t('HITS', searchResults.length || 0)),
            searchResults.length > 0 && [],
          ],
      ]),
    })
),
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix setSearchResults service with filtering logic</name>
  <files>/Users/erik.vullings/dev/mithril-app/packages/gui/src/services/meiosis.ts</files>
  <action>
  Update the setSearchResults service to implement actual filtering logic:
  - Get data from localStorage (model data)
  - Filter based on searchFilter against title, description, and content fields
  - Return filtered results array

  The filter should:
  - Match searchFilter against multiple fields (title, description, type)
  - Be case-insensitive
  - Handle empty filter (return empty or all based on UX decision)
  - Return results as SearchResults<unknown>
  </action>
  <verify>
  - Run `npm start` and test search in browser
  - Search for a term that exists in the model data
  - Verify results appear in the search dialog
  </verify>
  <done>
  - setSearchResults service filters data instead of returning empty array
  - Filter is case-insensitive and matches multiple fields
  - Results appear when typing in search input
  </done>
</task>

<task type="auto">
  <name>Task 2: Update search UI to display results and add clear button</name>
  <files>/Users/erik.vullings/dev/mithril-app/packages/gui/src/components/layout.ts</files>
  <action>
  Update the search dialog UI in layout.ts:
  - Add a clear button (X) to reset the search filter
  - Fix the results display section (currently has an issue - empty array condition)
  - Display results in a proper list format below the input
  - Add empty state message when no results found: "No results found"
  </action>
  <verify>
  - Clear button appears in search dialog
  - Clicking clear resets the input and filter
  - "No results found" message displays when search returns no matches
  - Results list shows items when filter matches
  </verify>
  <done>
  - Clear button implemented with onclick to reset searchFilter
  - Results display shows proper message when empty
  - Search filter updates in real-time as user types
  </done>
</task>

</tasks>

<verification>
- Search input accepts text and filters results dynamically
- Results appear in the modal dialog below the input field
- Empty search returns appropriate "No results found" message
- Clear button resets the search filter and clears results
</verification>

<success_criteria>
1. User can enter a search query and see filtered results - DONE when setSearchResults filters data
2. Search results update dynamically as query changes - DONE when onchange triggers service
3. Empty search returns appropriate message - DONE when "No results found" displays
</success_criteria>

<output>
After completion, create `.planning/phases/03-search-functionality/03-01-SUMMARY.md`
</output>
