---
phase: 02-settings-persistence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/erik.vullings/dev/mithril-app/packages/gui/src/services/meiosis.ts
autonomous: true
requirements:
  - SETT-01
  - SETT-02
  - SETT-03
must_haves:
  truths:
    - "Settings are automatically saved to localStorage when they change"
    - "Settings persistence service watches for state.settings changes"
    - "Settings load from localStorage on startup and persist across sessions"
  artifacts:
    - path: "/Users/erik.vullings/dev/mithril-app/packages/gui/src/services/meiosis.ts"
      provides: "Settings persistence service and configuration"
      exports: ["settingsSaveService", "actions", "cells"]
      contains: "export const settingsSaveService"
  key_links:
    - from: "/Users/erik.vullings/dev/mithril-app/packages/gui/src/services/meiosis.ts"
      to: "/Users/erik.vullings/dev/mithril-app/packages/gui/src/models/settings.ts"
      via: "Settings type dependency"
      pattern: "import.*Settings.*from.*models"
    - from: "/Users/erik.vullings/dev/mithril-app/packages/gui/src/services/meiosis.ts"
      to: "localStorage"
      via: "settingsSaveService persistence"
      pattern: "localStorage\\.setItem.*SETTINGS_KEY.*state\\.settings"
---

<objective>
Add settings service watcher to persist settings changes automatically

Purpose: Currently settings only save when saveSettings action is explicitly called. This plan adds a settings service that watches for settings changes and persists them to localStorage automatically, following the Meiosis pattern used by setSearchResults.

Output: Automatic settings persistence service that triggers on settings changes
</objective>

<execution_context>
@/Users/erik.vullings/dev/mithril-app/packages/gui/src/services/meiosis.ts
</execution_context>

<context>
@/Users/erik.vullings/dev/mithril-app/.planning/phases/02-settings-persistence/02-RESEARCH.md

## Current State After Plan 01

**Settings service pattern (from research):**
```typescript
export const saveSettings: Service<State> = {
  onchange: (state) => state.settings,
  run: (cell) => {
    const state = cell.getState();
    localStorage.setItem('SETTINGS_KEY', JSON.stringify(state.settings));
  },
};
```

**Existing services array (meiosis.ts line 104):**
```typescript
services: [setSearchResults],
```

**Current saveSettings action (lines 65-70):**
```typescript
saveSettings: async (cell: MeiosisCell<State>, settings: Settings) => {
  // await settingsSvc.save(settings);
  cell.update({
    settings: () => settings,
  });
},
```

**Settings key (after plan 01):**
```typescript
const SETTINGS_KEY = 'MITHRIL_APP_SETTINGS';
```
</context>

<tasks>

<task type="auto">
  <name>Task: Create settings persistence service and integrate</name>
  <files>/Users/erik.vullings/dev/mithril-app/packages/gui/src/services/meiosis.ts</files>
  <action>Create a settings persistence service that automatically saves settings to localStorage:
1. Create settingsSaveService after setSearchResults (around line 94):
   ```typescript
   export const settingsSaveService: Service<State> = {
     onchange: (state) => state.settings,
     run: (cell) => {
       const state = cell.getState();
       localStorage.setItem(SETTINGS_KEY, JSON.stringify(state.settings));
     },
   };
   ```
2. Add settingsSaveService to the services array in config (line 104)
3. The service will trigger on any settings change and persist to localStorage
4. Keep the saveSettings action as-is for explicit programmatic updates</action>
  <verify>settingsSaveService is defined and added to services array in config</verify>
  <done>Settings are automatically saved to localStorage when they change</done>
</task>

</tasks>

<verification>
- settingsSaveService service created following Meiosis pattern
- Service watches state.settings for changes
- Service saves to localStorage using SETTINGS_KEY
- Service added to services array in config
</verification>

<success_criteria>
Settings changes persist to localStorage automatically without requiring explicit saveSettings calls
</success_criteria>

<output>
After completion, create `.planning/phases/02-settings-persistence/02-02-SUMMARY.md`
</output>
